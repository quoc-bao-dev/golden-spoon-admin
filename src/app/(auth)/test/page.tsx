import { Icon } from "@/core/components/ui";

const Page = () => {
    return (
        <div>
            <Icon
                icon="icon-sidebar-4"
                className="size-[24px] hover:size-[54px] text-red-500 hover:text-gray-500 transition-all duration-200"
            />
        </div>
    );
};

export default Page;
